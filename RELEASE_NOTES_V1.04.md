# ZZZ Drive Optimizer V1.04

V1.04 统一下拉菜单的版本倒序，并提高代理人面板与专武数据的可读性。A/B 两版继续使用相同计算核心和库存结构。

## 修正内容

- 初次进入和清空配装条件时，不再硬编码回到“强攻 / 叶瞬光”；改为按全角色版本顺序自动选择最新版本代理人所属职业和该职业最新代理人。
- 代理人职业、职业内代理人、专武后续列表、驱动盘录入、库存套装筛选、4件套/2件套选择和本地方案筛选统一按版本倒序；自定义套装在内置套装之后按中文名称排列。
- 当前代理人的专属音擎仍优先自动匹配，避免为了全局排序而破坏配装操作流程。
- 代理人具体面板和专武数据框改为更大的 14px 主字体、15px 强调标题和以白色为主的高对比度显示；说明文字保留较弱层级但同步提高可读性。

## 历史记录

- 被删除的旧 GitHub Release 不等于删除修正历史。V1.0-V1.04 的问题、根因、修正和回归依据均保存在源码仓库的 `RELEASE_NOTES_*.md` 与 `CORRECTION_LOG.md`。
- V1.03 的蕾米埃尔核心技重复计入问题及两组实机回归继续保留；本版没有更改该计算口径。

## A/B 差异

- V1.04A：仅配装器，不含扫描器入口或离线扫描器文件。
- V1.04B：包含 ZZZ-Scanner.Next 1.0.45 自包含离线包，可从配装器直接启动。
