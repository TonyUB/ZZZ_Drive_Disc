# ZZZ Drive Optimizer v1.15

绝区零驱动盘配装器。v1.15 继续兼容 v1.14 库存和配装数据，新增调用独立视觉扫描器的一键扫描入口。

## v1.15 更新

- 页面新增“启动扫描器”按钮，调用独立维护的 ZZZ Scanner Next 1.0.45。
- 扫描器以外部组件运行；配装器不复制或修改其 OCR、窗口控制和兼容性实现。
- 完整扫描成功后自动读取 `export.json`，转换套装、槽位、等级和主副词条，并在用户确认后写入库存。
- 重复扫描按标准化词条指纹匹配；匹配成功时保留备注、废弃标记、锁定状态、装备角色和配装归属。
- 只替换此前由 ZZZ Scanner Next 导入的记录，手工录入及带游戏 UID 的协议扫描记录不会被删除。
- 状态版本升级为 `115`。

## v1.14 基线

### 佩洛伊斯面板状态适配

- 功能 3.1「代理人选择」新增“核心技 / 面板状态”下拉。
- 佩洛伊斯默认使用“未升级技能 / 实机截图校准”口径，用于贴近用户提供的 3.0 实机代理人信息截图。
- 该默认口径下：佩洛伊斯基础攻击按 911 参与最终攻击计算，异常精通基准按 120 参与计算，不额外计入核心技F的暴击率 +14.4%。
- 需要按资料站满核心F口径测试时，可手动切换到“核心技 F / MAX”。满核心资料口径保留为：生命 7673、攻击 924、防御 612、冲击力 94、暴击率 19.4%、暴击伤害 50%、异常精通 93、异常掌控 94。

### 拂晓行纪 4 件套面板口径修正

- 拂晓行纪 2 件套以太伤害 +10% 继续计入稳定属性。
- 拂晓行纪 4 件套的以太代理人暴击伤害 +30% 不再写入“面板暴伤”。
- 拂晓行纪 4 件套的触发攻击力 +10% 继续只作为实战攻击 / 排序参考。
- 结果摘要已更新为：角色60级成长、所选核心技/面板状态、音擎基础与高级属性、驱动盘主副属性和2件套静态效果计入角色详情面板；4件套和音擎技能类效果统一作为实战参考。

### 与 v1.13 的兼容

- 保留 v1.13 的库存筛选优化：功能 2 的“套装”筛选下拉只显示当前库存中实际存在的套装。
- 功能 1 的录入套装下拉、功能 3 的 4 件套 / 2 件套下拉仍保留完整套装池，便于录入与规划。
- 状态版本升级为 `114`，继续兼容旧库存 JSON、已存套装与驱动盘归属关系。

## 3.0 数据保留

- **维琳娜·艾嘉德**：S 级、风属性、异常、罗斯凯利法·外务筹策局。
- **佩洛伊斯**：S 级、以太属性、强攻、法厄同。
- **呼啸沙龙**：2 件套风属性伤害 +10%；4 件套异常精通与风化伤害作为实战参考。
- **拂晓行纪**：2 件套以太伤害 +10%；4 件套暴伤 +30% 和触发攻击力 +10%作为实战参考。
- **日冕遗蜕（佩洛伊斯）**：60 级基础攻击力 713，高级属性攻击力 30%；技能的 20%实战暴击率和精炼 1-5 的以太抗性无视已录入。

> 当前抗性无视、队伍条件、专属技能倍率、终结技分支等复杂战斗环境不直接写入通用期望指数。

## 使用方式

Windows 下双击 `ZZZ_Drive_Optimizer_v1.15.exe`。程序会启动本地服务并自动打开浏览器；黑色命令窗口关闭即退出程序。

## 扫描器与批量导入

点击页面中的“启动扫描器”会调用 `scan/external/ZZZ-Scanner.Next/ZZZ-Scanner.Next.exe`。完整发布包内置上游 1.0.45 self-contained 版本及本地 OCR 模型，不需要另装 .NET，也不需要联网识别。扫描前应打开游戏并停留在背包驱动盘页面，扫描期间保持游戏可见且不要操作鼠标。

视觉扫描器没有游戏内部 UID、锁定状态和装备角色字段；配装器使用套装、槽位、等级和全部词条生成匹配指纹。完全相同的多块驱动盘在物理上无法区分，但不影响配装计算。扫描器来源、使用说明与问题反馈见 [ZztIsolation/ZZZ-Scanner.Next](https://github.com/ZztIsolation/ZZZ-Scanner.Next)。

仓库中原有的 [`scan/`](scan/) 协议数据核心和 `scan-result.json` 手工导入入口继续保留。该入口按游戏内稳定 UID 写入，并严格校验区服、客户端版本与协议指纹。

真实登录/传输层必须使用与区服和客户端版本完全匹配的适配器。旧版或未经实机验证的协议会被拒绝，详见 [`scan/README.md`](scan/README.md)。

## 安全边界

配装器本体不读取游戏进程或内存、不修改游戏文件，也不接触登录凭据；它只接收用户确认的扫描结果、手动录入、截图或库存 JSON。独立扫描器只允许用户主动授权的一次性本地会话，不注入、不 Hook、不绕过反作弊，并且不会把 Token、Cookie、二维码票据或会话密钥写入导出文件或日志。

## 构建

```bash
go test ./...
go vet ./...
GOOS=windows GOARCH=amd64 go build -trimpath -ldflags "-s -w" -o ZZZ_Drive_Optimizer_v1.15.exe .
```
