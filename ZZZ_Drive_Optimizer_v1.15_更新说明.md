# ZZZ Drive Optimizer v1.15 更新说明

本版在 v1.14 原始配装器上增加独立视觉扫描器入口，原有配装算法、库存格式和协议扫描导入功能继续保留。

## 新增功能

1. `appVersion`：114 → 115。
2. “扫描器与批量导入”区域新增一个“启动扫描器”按钮。
3. 按钮调用 ZZZ Scanner Next 1.0.45 self-contained 外部组件，一次扫描全部符合条件的 S 级 +15 驱动盘。
4. 扫描成功后自动读取上游 `export.json`，转换中文套装、槽位、等级、主属性和副属性。
5. 写入前显示识别总数、新增、移除和匹配数量，由用户确认后才修改库存。
6. 重复扫描使用标准化词条指纹匹配；匹配记录会保留备注、废弃标记、锁定状态、装备角色、创建时间和配装归属。
7. 新扫描只替换 ID 以 `ocrscan:` 开头的视觉扫描记录，不删除手工录入或旧协议扫描记录。

## 外部组件

- 项目：[ZztIsolation/ZZZ-Scanner.Next](https://github.com/ZztIsolation/ZZZ-Scanner.Next)
- 固定兼容版本：1.0.45
- 运行方式：独立进程 `--scan-once`
- OCR：本机 PP-OCRv5 ONNX，不上传截图
- 发布包：Windows x64 self-contained

扫描器本身的识别、分辨率、HDR、游戏界面适配和故障排查由上游项目维护。配装器只负责启动、读取完整结果、转换和确认写入。

## 已知限制

- 视觉结果没有游戏内部驱动盘 UID。
- 锁定状态和装备角色不会从游戏中读取；匹配到旧记录时保留配装器里的人工信息，新记录默认为未锁定、未装备。
- 属性完全相同的多块驱动盘只能作为等价记录匹配，无法识别其物理顺序。
- 扫描失败、中止或生成 `export.partial.json` 时不会覆盖库存。

## 验证项目

- Go 单元测试与静态检查。
- 上游中文 JSON、百分比和固定数值转换测试。
- 重复扫描匹配与旧扫描记录替换测试。
- 手工库存、协议扫描库存及配装归属保留测试。
- 前端 JavaScript 语法检查。
- Windows amd64 发布构建与 SHA-256 校验。
