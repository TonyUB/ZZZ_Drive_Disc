# ZZZ Drive Optimizer v1.15 使用说明

## 启动配装器

1. 完整解压 `ZZZ_Drive_Optimizer_v1.15_release.zip`，不要只把主程序单独拖出压缩包。
2. 双击 `ZZZ_Drive_Optimizer_v1.15.exe`。
3. 保留黑色程序窗口；浏览器会自动打开配装器页面。

库存数据默认保存在 `%APPDATA%\ZZZDriveBuilder\state.json`，升级版本不会删除旧库存。

## 一键扫描驱动盘

1. 启动国服 Windows 版《绝区零》，使用简体中文界面。
2. 打开背包并停留在驱动盘页面，推荐使用扫描器正式支持的 16:9 分辨率。
3. 回到配装器页面，在“扫描器与批量导入”区域点击“启动扫描器”。
4. 确认提示后保持游戏可见，不要操作鼠标、切换窗口或遮挡游戏。
5. 扫描完成后，配装器会显示总数、新增、移除和匹配数量。
6. 确认写入后，库存和配装页面会自动刷新。

扫描器默认读取全部符合条件的 S 级 +15 驱动盘。扫描失败、中止或只生成部分结果时，配装器不会覆盖库存。

## 数据保留规则

- 只替换此前由 ZZZ Scanner Next 导入的视觉扫描记录。
- 手工录入、图片识别录入和带游戏 UID 的协议扫描记录不会被删除。
- 重复扫描匹配成功时，保留备注、废弃标记、锁定状态、装备角色和配装归属。
- 新扫描结果没有锁定状态和装备角色信息，因此新记录默认未锁定、未装备。
- 两块属性完全相同的驱动盘会作为等价记录处理，不影响配装计算。

## 扫描器组件

完整包内置 ZZZ Scanner Next 1.0.45 self-contained 版本：

`scan\external\ZZZ-Scanner.Next\ZZZ-Scanner.Next.exe`

OCR 模型随程序提供，识别在本机完成，不需要联网，也不需要另行安装 .NET。请不要移动、删除或只复制其中一个 EXE。

扫描器的分辨率、HDR、窗口识别和游戏版本兼容问题由上游项目维护：

https://github.com/ZztIsolation/ZZZ-Scanner.Next

## 常见问题

- **提示找不到扫描器**：确认完整解压，并检查上述组件路径是否存在。
- **扫描器找不到游戏窗口**：确认游戏客户端正在运行，且没有以高于配装器的权限启动。
- **HDR 或颜色过强导致失败**：先关闭游戏 HDR/Windows HDR 后重试，并将扫描日志反馈给上游维护者。
- **提示已有扫描任务**：等待当前扫描结束，不要连续启动多个实例。
- **写入前改变了库存**：重新扫描，避免把旧预览覆盖到新库存。

关闭配装器的黑色程序窗口即可退出本地服务。
